#include <stdio.h>
/**
 * main - print possible combination of two 2-digit
 * numbers
 *
 * Return: returns zero at the end
 */
int main(void)
{
 if (ni != n2 && ni n1 < n2)	
        {
            if (p > i)
            {
                putchar((i / 10) + '0');
                putchar((i % 10) + '0');

                putchar((n1 / 10) + 48);
                putchar((n1 % 10) + 48);
                putchar(' ');
                putchar((p / 10) + '0');
                putchar((p % 10) + '0');
                if (i != 98)
		putchar((n2 / 10) + 48);
		putchar((n2 % 10) +48);	

                if (n1 != 98 || n2 != 99)
                {
                    putchar(',');
                    putchar(' ');
                }
}
    ++n2;    
    }
   ++n1;

    }
putchar('\n');
    return (0);
}
